node_modules
dist
.DS_Store
*.local
__pycache__/

# video files — host on Cloudinary, never commit
*.mp4
*.mov
*.avi
*.webm
*.mkv

# raw source assets — processed versions live in public/, source stays local
assets/
.vercel

# Generated by scripts/extract-content-images.mjs before every dev run and
# build: the admin's uploaded images lifted out of the JS bundle into real
# files, and the snapshot rewritten to point at them. Both are derived from
# src/data/content.snapshot.json — never edit or commit them.
public/img/content/
src/data/content.snapshot.built.json
