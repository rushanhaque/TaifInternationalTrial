diff --git a/src/styles/mk2/theturn.css b/src/styles/mk2/theturn.css
index 4ab6b0d..69ed831 100644
--- a/src/styles/mk2/theturn.css
+++ b/src/styles/mk2/theturn.css
@@ -38,6 +38,13 @@
   padding-bottom: clamp(3rem, 6vh, 6rem);
   border-radius: clamp(18px, 2vw, 30px);
   background-color: var(--offwhite, #FDFCFA);
+  /* the house paper texture ΓÇö a fine hairline crosshatch already defined in
+     tokens.css at ~1% alpha, sitting unused everywhere else because .section
+     reads --paper, which is 'none' by sitewide default. Applied directly
+     here rather than by touching that shared variable, so only this one
+     card gets grain and nothing else on the site changes. It is meant to be
+     found, not seen ΓÇö closer than it looks, invisible at a glance. */
+  background-image: var(--paper-beige);
   border: 1px solid rgba(66, 21, 32, 0.12);
   box-shadow: 0 18px 50px rgba(0, 0, 0, 0.08);
   /* the pinned stage lives inside, so this must not clip or scroll-trap */
@@ -46,6 +53,7 @@
 
 html.dark .tt-card {
   background-color: var(--offwhite, #2C0C14);
+  background-image: var(--paper-deep);
   border-color: rgba(255, 255, 255, 0.16);
   box-shadow: 0 24px 70px rgba(0, 0, 0, 0.42);
 }
@@ -62,15 +70,6 @@ html.dark .tt-card {
   -webkit-text-fill-color: #1C1814 !important;
 }
 
-.tt .tt-note-tag {
-  background: #421520 !important;
-  color: #FFF6E0 !important;
-  -webkit-text-fill-color: #FFF6E0 !important;
-  padding: 0.4rem 1.1rem !important;
-  border-radius: 9999px !important;
-  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.14) !important;
-}
-
 /* The ink above is forced with !important and was left unscoped, so it also
    applied in dark mode ΓÇö where home.css drops this section onto a #1A090D
    ground. The whole Turn (headings, captions, the metal and wood spec lists)
@@ -140,7 +139,10 @@ html.dark .tt .ed-spec-label {
   display: flex;
   flex-direction: column;
   gap: clamp(0.6rem, 1.5vw, 1.2rem);
-  padding-block: calc(var(--nav-h) + clamp(0.5rem, 1.2vw, 1.2rem)) clamp(8rem, 14vw, 16rem) !important;
+  /* bottom trimmed from clamp(8rem, 14vw, 16rem) ΓÇö a shorter reserve under
+     the stage, which is most of what makes the whole section a bit taller
+     than it needs to be */
+  padding-block: calc(var(--nav-h) + clamp(0.5rem, 1.2vw, 1.2rem)) clamp(6rem, 11vw, 12.5rem) !important;
   color: var(--on-deep);
   background: transparent;
 }
@@ -240,7 +242,8 @@ html.dark .tt .ed-spec-label {
   height: var(--form);
   margin-inline: auto;
   margin-top: 0;
-  margin-bottom: clamp(6rem, 10vw, 12rem) !important;
+  /* trimmed from clamp(6rem, 10vw, 12rem) */
+  margin-bottom: clamp(4.5rem, 8vw, 9.5rem) !important;
   padding-inline: var(--gutter);
 }
 
@@ -248,7 +251,9 @@ html.dark .tt .ed-spec-label {
 
 .tt-form {
   position: absolute;
-  top: -36px;
+  /* still breaks up out of the rig, just less far than it originally did ΓÇö
+     the masthead above wants a little more air beneath it */
+  top: 44px;
   left: 50%;
   /* independent `translate` ΓÇö GSAP writes `transform`, so the two never
      fight and the scale breath keeps its centre through the dissolve */
@@ -559,26 +564,201 @@ html.dark .tt .ed-spec-label {
 
 .tt-note-body {
   display: block;
-  max-width: clamp(148px, 15vw, 238px);
+  max-width: clamp(200px, 19vw, 268px);
   padding-top: 0;
   border-top: none !important;
 }
 
-.tt .tt-note-tag {
+/* ΓöÇΓöÇ the identity card ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+   A callout used to be one maroon pill carrying the material's name, which
+   is why the plate read as empty: a 720px circle and a 60px badge. The card
+   below is the same callout with the rest of the information it always
+   implied ΓÇö what the stock chemically IS, what we call it, and how it
+   behaves in the hand. Text is left-aligned on BOTH sides; the side-aware
+   detail is the accent edge, which always faces the form it points at. */
+
+.tt-note-card {
+  position: relative;
   display: block;
-  margin-bottom: 0;
-  font-size: 0.625rem;
-  color: var(--tint-on-deep);
+  padding: clamp(0.8rem, 1.05vw, 1rem) clamp(0.95rem, 1.25vw, 1.2rem);
+  border-radius: 14px;
+  text-align: left;
+  background:
+    linear-gradient(180deg, rgba(255, 255, 255, 0.97), rgba(253, 252, 250, 0.92));
+  border: 1px solid rgba(66, 21, 32, 0.13);
+  box-shadow:
+    inset 0 1px 0 rgba(255, 255, 255, 0.9),
+    0 16px 38px -20px rgba(66, 21, 32, 0.5),
+    0 2px 6px rgba(66, 21, 32, 0.06);
+  -webkit-backdrop-filter: blur(6px);
+  backdrop-filter: blur(6px);
+}
+
+html.dark .tt-note-card {
+  background: linear-gradient(180deg, rgba(255, 255, 255, 0.09), rgba(255, 255, 255, 0.04));
+  border-color: rgba(255, 255, 255, 0.18);
+  box-shadow:
+    inset 0 1px 0 rgba(255, 255, 255, 0.12),
+    0 20px 46px -20px rgba(0, 0, 0, 0.7);
+}
+
+/* the edge that faces the circle ΓÇö a hairline of brand maroon, feathered at
+   both ends so it reads as light catching a rule rather than as a border */
+.tt-note-card::before {
+  content: '';
+  position: absolute;
+  top: 15%;
+  bottom: 15%;
+  width: 2px;
+  border-radius: 2px;
+  background: linear-gradient(180deg,
+    rgba(66, 21, 32, 0), rgba(66, 21, 32, 0.9) 45%, rgba(66, 21, 32, 0));
+}
+
+html.dark .tt-note-card::before {
+  background: linear-gradient(180deg,
+    rgba(255, 246, 224, 0), rgba(255, 246, 224, 0.75) 45%, rgba(255, 246, 224, 0));
+}
+
+.tt-note.side-l .tt-note-card::before { right: -1px; }
+.tt-note.side-r .tt-note-card::before { left: -1px; }
+
+.tt-note-head {
+  display: flex;
+  align-items: flex-start;
+  gap: 0.62rem;
+}
+
+/* the element tile ΓÇö the one saturated object in the card, so the eye lands
+   on the symbol first and reads the name second */
+.tt .tt-note-sym {
+  flex: none;
+  display: grid;
+  place-items: center;
+  /* MIN-WIDTH, NOT WIDTH ΓÇö a fixed square clipped every two-part formula
+     ("Cu┬╖Zn", "Fe┬╖C") because their glyphs are wider than one element's
+     height. min-width keeps single-letter symbols (Al, Fe, Cu) reading as a
+     square tile while letting the box grow to fit longer ones instead of
+     truncating them. */
+  min-width: 2.4rem;
+  height: 2.4rem;
+  padding-inline: 0.5rem;
+  border-radius: 10px;
+  background: linear-gradient(145deg, #5C2130 0%, #421520 55%, #2C0C14 100%);
+  color: #FFF6E0 !important;
+  -webkit-text-fill-color: #FFF6E0 !important;
+  font-family: var(--font-display);
+  font-weight: 600;
+  font-size: 0.78rem;
+  line-height: 1;
+  letter-spacing: 0.005em;
+  white-space: nowrap;
+  box-shadow:
+    inset 0 1px 0 rgba(255, 246, 224, 0.24),
+    0 3px 10px rgba(66, 21, 32, 0.34);
+}
+
+/* on the dark ground the maroon tile disappears into the card, so the
+   polarity flips: cream tile, maroon symbol */
+html.dark .tt .tt-note-sym {
+  background: linear-gradient(145deg, #FFF6E0 0%, #EBD6AE 100%);
+  color: #421520 !important;
+  -webkit-text-fill-color: #421520 !important;
+  box-shadow:
+    inset 0 -1px 0 rgba(66, 21, 32, 0.18),
+    0 3px 10px rgba(0, 0, 0, 0.4);
+}
+
+.tt-note-ident {
+  display: block;
+  min-width: 0;
 }
 
-.tt-note-text {
+.tt .tt-note-cls {
   display: block;
   font-family: var(--font-body);
-  font-size: clamp(0.82rem, 1.02vw, 0.98rem);
-  line-height: 1.36;
-  letter-spacing: -0.005em;
-  color: var(--on-deep);
-  text-wrap: balance;
+  font-size: 0.64rem;
+  font-weight: 600;
+  line-height: 1.2;
+  letter-spacing: 0.15em;
+  text-transform: uppercase;
+  color: rgba(66, 21, 32, 0.55) !important;
+  -webkit-text-fill-color: rgba(66, 21, 32, 0.55) !important;
+}
+
+html.dark .tt .tt-note-cls {
+  color: rgba(255, 246, 224, 0.58) !important;
+  -webkit-text-fill-color: rgba(255, 246, 224, 0.58) !important;
+}
+
+.tt .tt-note-name {
+  display: block;
+  margin-top: 0.16rem;
+  font-family: var(--font-display);
+  font-weight: 600;
+  font-size: clamp(1.14rem, 1.45vw, 1.42rem);
+  line-height: 1.02;
+  letter-spacing: -0.015em;
+  color: #421520 !important;
+  -webkit-text-fill-color: #421520 !important;
+}
+
+html.dark .tt .tt-note-name {
+  color: #FFFFFF !important;
+  -webkit-text-fill-color: #FFFFFF !important;
+}
+
+/* the counter ΓÇö 01/06. Present so a reader mid-scroll knows how much of the
+   turn is left, which the scrub itself never says. */
+.tt .tt-note-no {
+  margin-left: auto;
+  padding-top: 0.1rem;
+  font-family: var(--font-body);
+  font-size: 0.66rem;
+  font-weight: 600;
+  line-height: 1;
+  letter-spacing: 0.08em;
+  font-variant-numeric: tabular-nums;
+  white-space: nowrap;
+  color: rgba(66, 21, 32, 0.45) !important;
+  -webkit-text-fill-color: rgba(66, 21, 32, 0.45) !important;
+}
+
+html.dark .tt .tt-note-no {
+  color: rgba(255, 255, 255, 0.45) !important;
+  -webkit-text-fill-color: rgba(255, 255, 255, 0.45) !important;
+}
+
+.tt-note-no i {
+  font-style: normal;
+  opacity: 0.55;
+}
+
+.tt-note-rule {
+  display: block;
+  height: 1px;
+  margin: 0.72rem 0 0.6rem;
+  background: linear-gradient(90deg, rgba(66, 21, 32, 0.22), rgba(66, 21, 32, 0));
+}
+
+html.dark .tt-note-rule {
+  background: linear-gradient(90deg, rgba(255, 255, 255, 0.26), rgba(255, 255, 255, 0));
+}
+
+.tt .tt-note-text {
+  display: block;
+  font-family: var(--font-body);
+  font-size: clamp(0.8rem, 0.95vw, 0.9rem);
+  line-height: 1.55;
+  letter-spacing: -0.003em;
+  color: rgba(28, 24, 20, 0.74) !important;
+  -webkit-text-fill-color: rgba(28, 24, 20, 0.74) !important;
+  text-wrap: pretty;
+}
+
+html.dark .tt .tt-note-text {
+  color: rgba(255, 255, 255, 0.72) !important;
+  -webkit-text-fill-color: rgba(255, 255, 255, 0.72) !important;
 }
 
 .tt-note-line {
@@ -760,8 +940,9 @@ html.dark .tt .ed-spec-label {
 /* ΓòÉΓòÉΓòÉ PINNED ΓÇö desktop, motion allowed ΓòÉΓòÉΓòÉ */
 @media (min-width: 1000px) and (prefers-reduced-motion: no-preference) {
   .tt-stage {
-    height: 92svh;
-    min-height: 540px;
+    /* trimmed from 92svh / 540px */
+    height: 84svh;
+    min-height: 480px;
     padding-top: clamp(3.8rem, 6vh, 4.8rem);
     padding-bottom: 0.5rem;
   }
@@ -807,35 +988,83 @@ html.dark .tt .ed-spec-label {
   }
 }
 
-.tt-sec-head {
+/* ΓöÇΓöÇΓöÇ the section masthead ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+   Lives above the pinned stage, centred, and narrow enough that the lede
+   sets to a readable measure rather than running the full card width. */
+
+.tt-head {
   position: relative;
-  top: 0;
-  left: 0;
   z-index: 20;
-  width: 100%;
   display: flex;
-  justify-content: center;
+  flex-direction: column;
   align-items: center;
   text-align: center;
-  margin-top: clamp(1.8rem, 3.5vh, 3rem);
-  margin-bottom: clamp(2rem, 4vh, 3.2rem);
+  max-width: 62ch;
+  margin-inline: auto;
+  padding-inline: var(--gutter);
+  /* nudged down from clamp(2.6rem, 6vh, 4.6rem) ΓÇö a little more air above
+     the eyebrow before the masthead starts */
+  padding-top: clamp(3.2rem, 7vh, 5.4rem);
+}
+
+.tt .tt-head-eyebrow {
+  display: block;
+  color: rgba(66, 21, 32, 0.6) !important;
+  -webkit-text-fill-color: rgba(66, 21, 32, 0.6) !important;
 }
-.tt-sec-head .meta {
+
+html.dark .tt .tt-head-eyebrow {
+  color: rgba(255, 255, 255, 0.62) !important;
+  -webkit-text-fill-color: rgba(255, 255, 255, 0.62) !important;
+}
+
+.tt .tt-h {
+  margin: clamp(0.8rem, 1.8vh, 1.2rem) 0 0;
+  font-family: var(--font-display);
+  font-weight: 500;
+  font-size: clamp(1.65rem, 3.6vw, 3.1rem);
+  line-height: 1.04;
+  letter-spacing: -0.028em;
+  text-wrap: balance;
   color: #421520 !important;
   -webkit-text-fill-color: #421520 !important;
 }
-html.dark .tt-sec-head .meta {
+
+html.dark .tt .tt-h {
   color: #FFFFFF !important;
   -webkit-text-fill-color: #FFFFFF !important;
 }
-@media (max-width: 999.98px) {
-  .tt-sec-head {
-    position: relative;
-    top: 0;
-    left: 0;
-    margin-top: clamp(1.5rem, 3vh, 2.4rem);
-    margin-bottom: clamp(1.6rem, 3.2vh, 2.5rem);
-  }
+
+.tt .tt-h-lede {
+  margin: clamp(0.75rem, 1.6vh, 1.1rem) 0 0;
+  max-width: 54ch;
+  font-family: var(--font-body);
+  font-size: clamp(0.86rem, 1.05vw, 1rem);
+  line-height: 1.62;
+  text-wrap: pretty;
+  color: rgba(28, 24, 20, 0.68) !important;
+  -webkit-text-fill-color: rgba(28, 24, 20, 0.68) !important;
+}
+
+html.dark .tt .tt-h-lede {
+  color: rgba(255, 255, 255, 0.68) !important;
+  -webkit-text-fill-color: rgba(255, 255, 255, 0.68) !important;
+}
+
+/* a short centred rule closing the masthead ΓÇö the visual full stop between
+   the written introduction and the object it introduces */
+.tt-head-rule {
+  display: block;
+  width: clamp(48px, 7vw, 88px);
+  height: 1px;
+  margin-top: clamp(1.2rem, 3vh, 2rem);
+  background: linear-gradient(90deg,
+    rgba(66, 21, 32, 0), rgba(66, 21, 32, 0.55), rgba(66, 21, 32, 0));
+}
+
+html.dark .tt-head-rule {
+  background: linear-gradient(90deg,
+    rgba(255, 255, 255, 0), rgba(255, 255, 255, 0.5), rgba(255, 255, 255, 0));
 }
 
 /* ΓòÉΓòÉΓòÉ PINNED ΓÇö MOBILE ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ
@@ -857,15 +1086,19 @@ html.dark .tt-sec-head .meta {
    ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ */
 
 @media (max-width: 999.98px) and (prefers-reduced-motion: no-preference) {
-  /* the stage becomes a room again so there is something to pin */
+  /* the stage becomes a room again so there is something to pin ΓÇö trimmed
+     from 86svh */
   .tt-stage {
-    min-height: 86svh;
+    min-height: 78svh;
     justify-content: flex-start;
     padding-block: clamp(1.2rem, 4vh, 2.4rem) clamp(2rem, 6vh, 3.5rem) !important;
   }
 
+  /* the callout below the form is now a card, not a pill, so the room the
+     rig reserves beneath itself grew with it ΓÇö see the note on the second
+     margin-bottom further down, which is the one that actually lands */
   .tt-rig {
-    margin-bottom: clamp(7rem, 22vw, 10rem) !important;
+    margin-bottom: clamp(9.5rem, 30vw, 13rem) !important;
   }
 
   .tt-notes {
@@ -885,7 +1118,11 @@ html.dark .tt-sec-head .meta {
     right: auto;
     translate: -50% 0;
     justify-content: center;
-    width: max-content;
+    /* `max-content` sized to the longest unbroken run of text, which was
+       harmless for a one-word pill and is not for a card carrying a
+       sentence ΓÇö it would size to the whole sentence and overflow. The card
+       now takes a fixed, gutter-aware measure instead. */
+    width: min(30rem, calc(100vw - 2 * var(--gutter)));
     max-width: calc(100vw - 2 * var(--gutter));
   }
 
@@ -898,12 +1135,24 @@ html.dark .tt-sec-head .meta {
 
   .tt-note-body {
     max-width: none;
-    text-align: center;
+    width: 100%;
   }
 
-  .tt .tt-note-tag {
-    font-size: 0.95rem !important;
-    white-space: nowrap;
+  /* the accent edge faces the form on desktop, where the card sits beside
+     it. Centred beneath the form there is no "toward" any more, so it moves
+     to the leading edge and stays there for all six. */
+  .tt-note.side-l .tt-note-card::before,
+  .tt-note.side-r .tt-note-card::before {
+    left: -1px;
+    right: auto;
+  }
+
+  .tt .tt-note-name {
+    font-size: 1.24rem;
+  }
+
+  .tt .tt-note-text {
+    font-size: 0.84rem;
   }
 
   /* The spec list is the callouts' accessible twin ΓÇö on desktop it is
@@ -959,9 +1208,11 @@ html.dark .tt-sec-head .meta {
    ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ */
 
 @media (max-width: 999.98px) {
-  /* -36px + 40px */
+  /* 44px + 40px, then nudged up 12px more so the circle and the identity
+     card underneath it read as two distinct things with a touch more air
+     between them, mobile only ΓÇö the desktop plate never used this rule. */
   .tt-form {
-    top: 4px;
+    top: 72px;
   }
 }
 
@@ -972,12 +1223,13 @@ html.dark .tt-sec-head .meta {
   .tt-note.a4,
   .tt-note.a5,
   .tt-note.a6 {
-    top: calc(100% + 40px + clamp(1.4rem, 5vw, 2.4rem));
+    /* Push the card down enough to completely clear the circle, which is itself pushed down 72px */
+    top: calc(100% + 72px + clamp(1.5rem, 5vw, 2.4rem));
   }
 
   /* and the rig carries that much more beneath it, so the caption still has
-     its own air at the bottom of the turn */
+     its own air at the bottom of the turn. */
   .tt-rig {
-    margin-bottom: calc(clamp(7rem, 22vw, 10rem) + 40px) !important;
+    margin-bottom: calc(clamp(9.5rem, 30vw, 13rem) + 72px) !important;
   }
 }
